<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.conversorunidadesfo">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Fibromarket"
        android:supportsRtl="true"
        android:theme="@style/Theme.ConversorUnidadesFO">
        <activity android:name=".MainActivity">
            android:parentActivityName=".inicio"
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".inicio" />
        </activity>


        <activity android:name=".ato_350">
            android:parentActivityName=".instructivos">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".instructivos" />
        </activity>
        <activity
            android:name=".ato_750"
            android:parentActivityName=".instructivos">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".instructivos" />
        </activity>
        <activity
            android:name=".pdf_ats80"
            android:parentActivityName=".instructivos">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".instructivos" />
        </activity>
        <activity
            android:name=".pdf_kl280e"
            android:parentActivityName=".instructivos">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".instructivos" />
        </activity>
        <activity android:name=".instructivos">
            android:parentActivityName=".inicio">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".inicio" />
        </activity>
        <activity
            android:name=".contactos"
            android:parentActivityName=".inicio">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".inicio" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".visualizadorWeb">
            android:parentActivityName=".inicio" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".inicio" />
        </activity>
        <activity android:name=".inicio">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".dbm_mw"
            android:parentActivityName=".inicio">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".inicio" />
        </activity>
    </application>

</manifest>